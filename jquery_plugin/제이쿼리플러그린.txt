* 플러그인 사용시 주의사항
 - jquery 라이브러리 import 했는지 체크.
 - 플러그인 js import 했는지 체크.
 - jquery 라이브러리 import 후 플러그인을 import 하고, 플러그인 호출하는 스크립트는 그 다음에 작성해야함.

* 플러그인 사용법(플러그인 마다 다르지만 대부분)
$("선택자").플러그인명(); -> 기본실행
$("선택자").플러그인명({
    옵션1 : 옵션값1
    옵션n : 옵션값n
    옵션3 : {
        옵션1 : 옵션값n
    }
});









